/**
 * The Layer interface is used for each canvas layer
 * @param {string} id An unique identifier for this layer
 * @param {string} nane The name of the layer
 */
export default interface Layer {
  id: string
  name: string
}